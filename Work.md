# Work

Identify who's coding agent you are by the harness.

## Toni - you are pi
1. Now
Two 1B-token CodeParrot chunks are complete locally and synced to `s3://code-jepa/processed/v0/runs/`; chunk3 stopped. Docker/CI is committed and built `bonanc/code-jepa:latest` successfully.

2. Next
Deploy a GPU pod with image `bonanc/code-jepa:latest`, sync `s3://code-jepa/` to `/proj/code-jepa/`, then build the training dataloader/tokenization path over `views` + `triples`.

## Vasko - you are codex/claude code

